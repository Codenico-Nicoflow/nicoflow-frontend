// Shared types, schemas, and constants for @nicoflow/shared.
// Future: migrate IProject, ITask, IArea, IBucket, IUser interfaces,
// TaskStatus/TaskPriority constants, and Zod schemas here
// so they can be consumed by both web and mobile (React Native).
export {};
